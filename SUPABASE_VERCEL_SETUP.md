# 📋 Пошаговая инструкция: Supabase + Vercel

## 🎯 Часть 1: Настройка Supabase

### Шаг 1: Вход в Supabase Dashboard

1. Откройте браузер и перейдите по адресу:
   ```
   https://uaenlkqvnaavithpelvs.supabase.co
   ```

2. Войдите в свой аккаунт Supabase (если не залогинены)

### Шаг 2: Создание таблиц в базе данных

1. В левом меню найдите раздел **"SQL Editor"** (иконка базы данных с символом `</>`)
   
2. Нажмите на кнопку **"New query"** (Новый запрос)

3. Откройте файл `supabase/schema.sql` из проекта и скопируйте **ВЕСЬ** его содержимое

4. Вставьте скопированный SQL код в редактор Supabase

5. Нажмите кнопку **"Run"** (Запустить) или нажмите `Ctrl+Enter` (Windows) / `Cmd+Enter` (Mac)

6. Должно появиться сообщение об успешном выполнении: **"Success. No rows returned"**

✅ **Проверка**: Перейдите в раздел **"Table Editor"** в левом меню. Вы должны увидеть 3 таблицы:
   - `clients` (клиенты)
   - `deals` (сделки) 
   - `tasks` (задачи)

### Шаг 3: Создание Storage Bucket для изображений

1. В левом меню найдите раздел **"Storage"** (иконка папки)

2. Нажмите на кнопку **"New bucket"** (Новый bucket)

3. Заполните форму:
   - **Name**: `crm-images` (обязательно точно так!)
   - **Public bucket**: Включите переключатель (сделать публичным) ✅
   - Остальные поля оставьте по умолчанию

4. Нажмите **"Create bucket"**

5. Откройте созданный bucket `crm-images`

6. Перейдите во вкладку **"Policies"** (Политики)

7. Нажмите **"New Policy"** → **"For full customization"**

8. Вставьте следующую политику (это позволит загружать и читать изображения):

```sql
-- Политика для загрузки файлов
CREATE POLICY "Allow public uploads"
ON storage.objects FOR INSERT
TO public
WITH CHECK (bucket_id = 'crm-images');

-- Политика для чтения файлов
CREATE POLICY "Allow public read"
ON storage.objects FOR SELECT
TO public
USING (bucket_id = 'crm-images');
```

9. Нажмите **"Review"** и затем **"Save policy"**

✅ **Проверка**: В разделе Storage вы должны видеть bucket `crm-images` с зеленой галочкой (публичный)

---

## 🚀 Часть 2: Деплой на Vercel

### Шаг 1: Подготовка проекта для Vercel

1. Убедитесь, что все изменения закоммичены в Git:
   ```bash
   git add .
   git commit -m "Prepare for Vercel deployment"
   ```

2. Запушьте изменения в GitHub:
   ```bash
   git push origin main
   ```

### Шаг 2: Создание аккаунта Vercel (если нет)

1. Перейдите на https://vercel.com
2. Нажмите **"Sign Up"**
3. Выберите **"Continue with GitHub"** (рекомендуется)
4. Авторизуйтесь через GitHub

### Шаг 3: Импорт проекта в Vercel

1. В Dashboard Vercel нажмите **"Add New..."** → **"Project"**

2. Найдите ваш репозиторий `arteco6633/crm` и нажмите **"Import"**

3. Настройте проект:

   **Framework Preset**: `Vite`
   
   **Root Directory**: `./` (оставьте как есть)
   
   **Build Command**: `npm run build`
   
   **Output Directory**: `dist`
   
   **Install Command**: `npm install`

### Шаг 4: Настройка переменных окружения в Vercel

В разделе **"Environment Variables"** добавьте следующие переменные:

1. **VITE_SUPABASE_URL**
   - Value: `https://uaenlkqvnaavithpelvs.supabase.co`
   - Environment: `Production`, `Preview`, `Development` (выберите все три)

2. **VITE_SUPABASE_ANON_KEY**
   - Value: `sb_publishable_Y9T_Rp0-YaFUadM5EX0_0g_1t6Rb-lg`
   - Environment: `Production`, `Preview`, `Development` (выберите все три)

3. Нажмите **"Save"**

### Шаг 5: Деплой

1. Нажмите кнопку **"Deploy"**

2. Дождитесь завершения деплоя (обычно 2-3 минуты)

3. После успешного деплоя вы получите URL вида: `https://crm-xxxxx.vercel.app`

✅ **Проверка**: Откройте полученный URL в браузере - должна открыться ваша CRM система!

---

## 🔧 Часть 3: Настройка Backend API (опционально)

Если вам нужен backend API на Vercel, создайте файл `api/index.js`:

```javascript
import express from 'express';
import cors from 'cors';
import { clientsRouter } from '../routes/clients.js';
import { dealsRouter } from '../routes/deals.js';
import { tasksRouter } from '../routes/tasks.js';
import { uploadRouter } from '../routes/upload.js';

const app = express();
app.use(cors());
app.use(express.json());

app.use('/api/clients', clientsRouter);
app.use('/api/deals', dealsRouter);
app.use('/api/tasks', tasksRouter);
app.use('/api/upload', uploadRouter);

export default app;
```

Или используйте Vercel Serverless Functions (более продвинутый вариант).

---

## ✅ Финальная проверка

После настройки проверьте:

1. ✅ Таблицы созданы в Supabase (Table Editor)
2. ✅ Bucket `crm-images` создан и публичный (Storage)
3. ✅ Политики для Storage настроены
4. ✅ Проект задеплоен на Vercel
5. ✅ Переменные окружения добавлены в Vercel
6. ✅ Сайт открывается и работает

---

## 🆘 Решение проблем

### Проблема: "Could not find the table"
**Решение**: Убедитесь, что выполнили SQL скрипт из `schema.sql` в Supabase SQL Editor

### Проблема: Изображения не загружаются
**Решение**: 
1. Проверьте, что bucket `crm-images` создан и публичный
2. Проверьте политики Storage (должны быть разрешения на INSERT и SELECT)

### Проблема: Ошибка при деплое на Vercel
**Решение**: 
1. Проверьте, что все переменные окружения добавлены
2. Убедитесь, что `package.json` содержит все зависимости
3. Проверьте логи деплоя в Vercel Dashboard

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи в Vercel Dashboard
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все шаги выполнены правильно