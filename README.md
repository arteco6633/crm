# Modern CRM System

Современная минималистичная CRM система на базе React, Vite и Supabase с Kanban доской для сделок.

## ✨ Возможности

- 🎨 **Современный минималистичный дизайн** - чистый и интуитивный интерфейс
- 📊 **Kanban доска для сделок** - drag & drop управление сделками
- 👥 **Управление клиентами** - полный CRUD для клиентской базы
- ✅ **Управление задачами** - отслеживание задач с приоритетами
- 🖼️ **Загрузка изображений** - поддержка изображений для сделок через Supabase Storage
- 📱 **Адаптивный дизайн** - работает на всех устройствах

## 🚀 Технологии

- **Frontend**: React 18, Vite
- **UI**: Минималистичный дизайн с CSS переменными
- **Drag & Drop**: @dnd-kit для Kanban доски
- **Icons**: Lucide React
- **Backend**: Node.js + Express
- **Database**: Supabase (PostgreSQL)
- **Storage**: Supabase Storage для изображений

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone git@github.com:arteco6633/crm.git
cd crm
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в корне проекта:
```env
VITE_SUPABASE_URL=https://uaenlkqvnaavithpelvs.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_Y9T_Rp0-YaFUadM5EX0_0g_1t6Rb-lg

SUPABASE_URL=https://uaenlkqvnaavithpelvs.supabase.co
SUPABASE_ANON_KEY=sb_publishable_Y9T_Rp0-YaFUadM5EX0_0g_1t6Rb-lg

PORT=3000
```

## 🗄️ Настройка базы данных

1. Откройте [Supabase Dashboard](https://uaenlkqvnaavithpelvs.supabase.co)

2. Перейдите в **SQL Editor** и выполните SQL скрипт из файла `supabase/schema.sql`

3. Создайте Storage bucket для изображений:
   - Перейдите в **Storage** → **Buckets**
   - Создайте новый bucket с именем `crm-images`
   - Установите публичный доступ (Public bucket)

## 🏃 Запуск

### Разработка

Запустите фронтенд и backend одновременно:

```bash
# Терминал 1 - Backend API
npm run server:dev

# Терминал 2 - Frontend (Vite)
npm run dev
```

Frontend будет доступен на http://localhost:5173
Backend API на http://localhost:3000

### Продакшн

```bash
npm run build
npm run preview
npm run server
```

## 📁 Структура проекта

```
crm/
├── src/
│   ├── components/
│   │   ├── Clients.jsx          # Компонент клиентов
│   │   ├── Clients.css
│   │   ├── DealsKanban.jsx      # Kanban доска для сделок
│   │   ├── DealsKanban.css
│   │   ├── Tasks.jsx            # Компонент задач
│   │   └── Tasks.css
│   ├── App.jsx                  # Главный компонент
│   ├── App.css
│   ├── main.jsx                 # Точка входа
│   └── index.css                # Глобальные стили
├── routes/
│   ├── clients.js               # API для клиентов
│   ├── deals.js                 # API для сделок
│   ├── tasks.js                 # API для задач
│   └── upload.js                # API для загрузки изображений
├── config/
│   └── supabase.js              # Конфигурация Supabase
├── supabase/
│   └── schema.sql               # SQL схема БД
├── server.js                    # Express сервер
├── vite.config.js               # Конфигурация Vite
└── package.json
```

## 🎯 Основные функции

### Kanban доска для сделок
- Drag & drop между колонками (стадиями)
- Визуальное отображение стадий сделок
- Поддержка изображений для каждой сделки
- Отображение суммы, вероятности и даты закрытия

### Управление клиентами
- Карточки клиентов с аватарами
- Фильтрация по статусу
- Полная информация о контактах

### Управление задачами
- Чекбоксы для отметки выполнения
- Приоритеты (высокий, средний, низкий)
- Фильтрация по статусу
- Связь с клиентами и сделками

## 🔌 API Endpoints

### Клиенты
- `GET /api/clients` - получить всех клиентов
- `GET /api/clients/:id` - получить клиента
- `POST /api/clients` - создать клиента
- `PUT /api/clients/:id` - обновить клиента
- `DELETE /api/clients/:id` - удалить клиента

### Сделки
- `GET /api/deals` - получить все сделки
- `GET /api/deals/:id` - получить сделку
- `POST /api/deals` - создать сделку
- `PUT /api/deals/:id` - обновить сделку
- `DELETE /api/deals/:id` - удалить сделку

### Задачи
- `GET /api/tasks` - получить все задачи
- `GET /api/tasks/:id` - получить задачу
- `POST /api/tasks` - создать задачу
- `PUT /api/tasks/:id` - обновить задачу
- `DELETE /api/tasks/:id` - удалить задачу

### Загрузка изображений
- `POST /api/upload` - загрузить изображение (multipart/form-data)

## 🎨 Дизайн

Система использует минималистичный современный дизайн с:
- Чистой типографикой
- Мягкими тенями и скруглениями
- Плавными анимациями
- Интуитивной навигацией
- Адаптивной версткой

## 📝 Лицензия

MIT